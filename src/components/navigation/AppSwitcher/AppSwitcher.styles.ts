import { MenuGridIcon } from '@src/components/icons'
import styled from 'styled-components'

export const StyledMenuGridIcon = styled(MenuGridIcon)`
	width: 28px;
	height: auto;
	cursor: pointer;
`

export const GroupTitle = styled.p`
	padding: 0.5rem 1rem;
	font-weight: 500;
`
