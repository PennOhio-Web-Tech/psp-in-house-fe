import styled from 'styled-components'

export const Input = styled.input`
	padding: 1rem;
	box-shadow: 0px 4px 16px #6a6a6a1a;
	border: 1px solid rgba(0, 0, 0, 0.15);
	border-radius: 4px;
	color: black;
	font-size: 0.9rem;

	@media (max-width: 1280px) {
		font-size: 0.75rem;
	}
`
