import styled from 'styled-components'

export const TimeExample = styled.p`
	color: ${({ theme }) => theme.colors.text.secondary};
	font-size: 0.9rem;
	position: absolute;
	top: 100%;
	right: 0;
	width: max-content;
`
