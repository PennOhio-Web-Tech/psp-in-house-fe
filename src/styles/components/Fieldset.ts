import styled from 'styled-components'

export const Fieldset = styled.fieldset`
	all: unset;
	display: flex;
	flex-direction: column;
	gap: 0.25rem;
	margin-bottom: 2rem;
	width: 100%;
	position: relative;
`
